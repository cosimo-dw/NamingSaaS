<% if @order_type == 'Personal' or @order_type == 'Company' %>
    名字
    <div>
      <%= f.text_field :chosen_name, :id => 'answer_name' %>
    </div>
<% end %>
详细分析
<div>
  <%= f.text_area :content, :id => 'answer_explanation' %>
</div>
<%= f.hidden_field :order_id, :value => @order.id %>
<%= submit_tag "提交", :class => "btn  btn-primary" %>